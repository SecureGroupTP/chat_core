name: sgtp_chat_core
description: Flutter/Dart FFI bindings and native assets for chat_core.
version: 0.1.1
publish_to: 'none'

environment:
  sdk: '>=3.10.0 <4.0.0'
  flutter: '>=3.38.0'

dependencies:
  code_assets: ^1.0.0
  crypto: ^3.0.6
  flutter:
    sdk: flutter
  flutter_rust_bridge: ^2.12.0
  hooks: ^1.0.0
